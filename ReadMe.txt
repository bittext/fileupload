ReadMe
=============

Rest API to upload a file with few meta data information stored to a H2 database and the file saved to a file system

#For compiling run command
 > mvn spring-boot:run

#For running the application 
   url > http://localhost:8080/ 

